// Exemplo 1:

// Native...Objetos nativos são aqueles definidos na especificação da linguagem e são implementados independente do host...

// Exemplo 2:

// User...Objetos do user, são os objetos definidos pelo seu aplicativo. Ou seja, qualquer objeto que você criar ou que importar de alguma biblioteca externa...

// Exemplo 3:

// Bibliotecas...Bibliotecas como jQuery foram criadas para resover o problema de inconsistências entre browsers e adicionar funcionalidades que não existiam nativamente...

// A padronização dos browsers e a implementação de soluções nativas, torna as mesmas obsoletas...

// Exemplo 4:

// Verificar se Existe...O typeof retorna o tipo de dado. Caso esse dado não existe ou não exista ou não tenha sido definido, ele irá reotrnar undefined. Ou seja, quando não for undefined que dizer que existe...

console.log(typeof Array.from !== "undefined")


if (typeof Array.ffsfsf !== "undefined") {

    console.log('Existe')
} else {
    
    console.log('Não existe')
}